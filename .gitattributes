# Auto detect text files and perform LF normalization
* text=auto

<head>
    <!-- existing code -->
    <style>
        .sticky {
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 100;
        }
    </style>
</head>

<header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom sticky">
    <!-- existing code -->
</header>